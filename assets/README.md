Place the photo you attached (kaligua) here with the filename `kaligua.jpg` so the site can use it for the page background.

Expected path: `assets/kaligua.jpg`

If you prefer a different filename, update `style.css` (background-image) accordingly.